export function authHeader(headers) {
    const user = JSON.parse(localStorage.getItem('user'));
    return (user && user.token) ? { Authorization: 'Bearer ' + user.token, ...headers } : { ...headers };
  }